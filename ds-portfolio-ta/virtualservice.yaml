apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ds-portfolio-ta
  namespace: ds-portfolio-ta
spec:
  hosts:
  - ds-portfolio-ta.apps.lab3.dslee.lab
  gateways:
  - istio-system/istio-gateway
  http:
  - match:
    - uri: { prefix: "/" }
    route:
    - destination:
        host: ds-portfolio-ta-service.ds-portfolio-ta.svc.cluster.local
        port: { number: 80 }
